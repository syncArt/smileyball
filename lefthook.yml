pre-commit:
  parallel: true
  commands:
    # Backend (Rust)
    cargo_fmt:
      run: "cargo fmt --all"
      glob: "backend/**/*.rs"
      root: "backend"

    cargo_clippy:
      run: "cargo clippy --all-targets --all-features -- -D warnings"
      glob: "backend/**/*.rs"
      root: "backend"

    # Frontend (JavaScript/TypeScript)
    lint_frontend:
      run: "npm run lint"
      glob: "frontend/**/*.{js,ts,tsx}"
      root: "frontend"

    prettier_frontend:
      run: "npm run prettier --write"
      glob: "frontend/**/*.{js,ts,tsx,css,scss,html}"
      root: "frontend"